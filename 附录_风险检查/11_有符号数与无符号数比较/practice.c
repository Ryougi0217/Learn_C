#include <stdio.h>

int main(void)
{
    int index = -1;
    size_t count = 3;

    if (/* TODO：先判断非负，再转换比较 */) {
        puts("index is valid");
    } else {
        puts("index is invalid");
    }

    return 0;
}

