# 第 14 课：分支、循环与函数验收

本课不新增脚本。请使用 CH2 前 13 课的练习文件，完成一次综合验收，并保存命令、输入、输出和解释。

## 任务 A：分支

使用第 1、2 课：

- [ ] 能处理 `if`/`else if`/`else` 的边界；
- [ ] 能解释 `switch`、`case`、`break` 和 `default`；
- [ ] 能复现一次意外贯穿并修复；
- [ ] 能说明什么时候范围条件更适合 `if`。

## 任务 B：循环

使用第 3～6 课：

- [ ] 能说明 `for` 的初始化、条件和更新；
- [ ] 能处理 `while` 初始条件为假的情况；
- [ ] 能证明 `do while` 至少执行一次；
- [ ] 能使用 `break` 和 `continue`，并保证循环终止；
- [ ] 能指出一个循环的状态变量和不变量直觉。

## 任务 C：函数

使用第 7～11 课：

- [ ] 能区分声明、定义和调用；
- [ ] 能使用参数和返回值；
- [ ] 能解释 C 的按值传递；
- [ ] 能区分局部变量、全局变量、作用域和生命周期；
- [ ] 能画出或口述一次递归调用链；
- [ ] 能说明函数拆分后的职责边界。

## 任务 D：算法和项目

使用第 12、13 课：

- [ ] 能为三个基础算法写出输入、处理、输出；
- [ ] 能完成边界测试；
- [ ] 能运行菜单项目的所有测试矩阵；
- [ ] 能解释菜单循环为什么不会在合法输入下提前退出；
- [ ] 能解释输入失败时为什么返回非零状态。

## 最终问答

请不看目录，独立回答：

1. `break` 和 `continue` 对循环控制流的影响分别是什么？
2. `for`、`while` 和 `do while` 如何选择？
3. 函数声明、定义、调用和链接之间有什么关系？
4. C 的按值传递为什么不会直接修改调用者的普通变量？
5. 作用域和生命周期有什么区别？
6. 递归函数缺少基础情况或参数不推进会发生什么？
7. 如何判断一个函数是否承担了过多职责？
8. 菜单项目中，输入错误、未知选项和用户选择退出分别应该如何处理？

## 通过标准

- [ ] A～D 均有实际运行记录；
- [ ] 能独立编译并运行至少 8 个练习；
- [ ] 能完成并测试命令行菜单项目；
- [ ] 能用代码输出或诊断信息证明自测答案；
- [ ] 能记录一个仍不熟悉的点和下一步验证方法。
